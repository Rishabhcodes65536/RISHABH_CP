#include <bits/stdc++.h>

using namespace std;

int main(){
    int n, s;
    cin >> n >> s;
    vector<int> v(n);
    vector<pair<int, int>> vep;
    for (int i = 0; i < n;i++){
        cin >> v[i];
        vep.push_back({v[i], i});
    }

}

//n*n 